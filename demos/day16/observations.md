# Day 16 实验观察记录

> README.md 已改为真实 Next.js App Router 实验（之前是纯 JS 模拟，无法真正体现 Server/Client 边界）。跑完后把真实结果填进下方区块。

## 浏览器版实测记录（待填）

### R1：Server Component 零 JS
- `getUser` 函数体是否出现在浏览器收到的任何 JS 文件里？
- `[Server]` 日志出现在终端还是浏览器 Console？`[Client]` 日志呢？

（待填）

### R2：序列化边界
- 传函数 `onIncrement` 时报了什么错误？
- 换成普通对象/字符串后是否正常？

（待填）

### R3：RSC payload vs 传统 SSR
- 首次刷新收到的响应是完整 HTML 还是别的格式？
- 客户端导航时，Network 面板里请求的 Content-Type 是什么？

（待填）
