# Day 22 实验观察记录

> 真实 Vite + React 项目浏览器实验。跑完后把真实结果填进下方区块。

## 浏览器版实测记录（待填）

### M1：selector 细粒度订阅验证
- 改 count 时，NameDisplay 是否真的没有重渲染？
- 改 name 时，CountDisplay 是否真的没有重渲染？

（待填）

### M2：批量更新去重验证
- 3 次同步 setState 是否只触发了 1 次"收到通知"？
- "3次setState调用完毕"和"收到通知"两行日志的先后顺序，是否验证了 notify 是在同步代码跑完之后才执行（微任务时序）？

（待填）

## 跟练中已修复的 bug

- 最初 `setState` 实现忘记调用 `notify()`，导致 `state` 数据本身已经正确更新，但订阅者（组件）永远不知道数据变了，页面上的值不会跟着更新。加上 `notify()` 调用后修复。
