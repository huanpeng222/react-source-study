# React Source Study · 执行规范（STUDY_PROTOCOL.md）

> **本文件是 AI 协作契约**。每次开始新一天学习时，AI 助手必须先读本文件，再读 `notes/day{N-1}.md`，然后才进入当天教学。
>
> 学习者在多台电脑切换，全部进度通过 git 接力，**不要重新设置任何规则**。

---

## 一、学习者画像（AI 必读）

- **角色**：5 年经验前端工程师，想吃透 React 源码 + AI Agent 工程实践。
- **技术底子**：React 完整生态（Hooks/Router/Redux/Zustand）、Vite、TypeScript、Webpack/Rspack 构建优化；用过但没系统读过 React 源码。
- **学习目标**：React 源码 → AI Agent 前端工程化 → 综合项目落地。
- **沟通语言**：简体中文。
- **学习风格**：
  - **教练式引导优先**，不要直接灌答案。
  - 先讲思路 / 取舍 / 心智模型，再贴代码。
  - 每个抽象概念配 1 个可跑实验。
  - 喜欢"我之前以为…，其实是…"这种认知纠正格式。
  - 喜欢类比、图解、表格、口诀。
- **历史踩坑**：
  - 反射性写 `const` 然后下面又赋值 → **建议默认先用 `let`**。
  - 大小写 / 拼写：`ReactDom` → `ReactDOM`、`CreateElement` → `createElement`、`ClassName` → `className`。
  - 链表题：忘 `return dummy.next`、比较方向反、`if` 忘加花括号。

---

## 二、仓库结构（强制约定）

```
react-source-study/
├── STUDY_PROTOCOL.md          ← 本文件（AI 协作契约）
├── README.md                  ← 总进度看板（每天结束更新）
├── notes/
│   ├── day1.md                ← 当日完整教程（教案，最长，跟练前先看）
│   ├── day1-summary.md        ← 当日精简笔记（含跟练疑问 + 踩坑，学完后写）
│   ├── day1-quiz.md           ← 当日自测题（答案折叠）
│   ├── day2.md
│   ├── day2-summary.md
│   ├── day2-quiz.md
│   └── ...
├── demos/
│   ├── day1/                  ← 每天一个文件夹，能跑
│   │   ├── README.md          ← 实验指引（怎么跑）
│   │   ├── index.html / src/  ← 代码
│   │   ├── observations.md    ← 实验观察记录
│   │   └── screenshots/       ← 截图
│   ├── day2/
│   └── ...
└── meta/
    ├── roadmap.md             ← 总学习路线图
    └── cognitive-corrections.md  ← 累计的"我之前以为…其实是…"
```

### 当日产物 4 件套（强制）

| 文件 | 时机 | 长度 | 内容 |
|---|---|---|---|
| `notes/dayN.md` | **跟练前/中输出** | 长（可上千行） | 完整教程：理论 + 源码 + 类比 + 示例 + 验收清单 + 次日预告 |
| `demos/dayN/` | **跟练中输出** | — | 可跑代码 + observations.md + 截图 |
| `notes/dayN-summary.md` | **跟练结束后输出** | 短（200-400 行） | 精简笔记：核心要点 + 跟练时的疑问 + 踩坑 + 我的认知纠正 |
| `notes/dayN-quiz.md` | **跟练结束后输出** | 短 | 6-10 题自测（答案折叠） |

⭐ **`dayN.md` 和 `dayN-summary.md` 的区别**：
- `dayN.md` 是**给未来的我看的教程**——什么都讲清楚，适合复习时从头读。
- `dayN-summary.md` 是**给现在的我看的速查卡**——只留要点 + 我亲手踩过的坑，适合面试前快速过一遍。

**禁止**：
- ❌ 笔记和 demo 混在一个文件里。
- ❌ 把 `node_modules` push 进仓库（用 `.gitignore`）。
- ❌ 在 `notes/dayN.md` 里贴大段截图（截图存到 `demos/dayN/screenshots/`）。
- ❌ 跳过 `dayN-summary.md`——这是面试时唯一会回看的文件。

---

## 三、每日 Day 流程（AI 必走）

```
┌─────────────────────────────────────────────┐
│ Step 0 · 接力对齐（无声进行，不打扰学习者）     │
│   • git pull                                 │
│   • 读 STUDY_PROTOCOL.md                     │
│   • 读 notes/day{N-1}.md（关注末尾"Day N 预告"）│
│   • 读 notes/day{N-1}-summary.md（如有）     │
│   • 读 meta/roadmap.md                       │
└─────────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────────┐
│ Step 1 · 写完整教程（notes/dayN.md）          │
│   学习者一句"开始第 N 天"，AI 立即输出完整教程： │
│   • 入场自测题（≥3）                          │
│   • 理论 + 源码 + 类比 + 示例                 │
│   • 实验任务清单 → 指向 demos/dayN/           │
│   • 5 条"我之前以为…其实是…"                 │
│   • Day N+1 预告（含预读问题）                │
│   ★ commit & push（学习者跟练前）            │
└─────────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────────┐
│ Step 2 · 入场自测（5 分钟）                   │
│   学习者口头/文字回答自测题，不查资料。        │
│   AI 根据回答深度决定后续展开节奏。            │
│   ★ AI 全程记录"学习者答错/疑问/踩坑"，         │
│     供 Step 5 写入 summary。                  │
└─────────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────────┐
│ Step 3 · ★系统完整讲解（主动，不靠追问驱动）★  │
│   AI 按 dayN.md 章节顺序【主动一节一节完整讲】，│
│   每节都讲全：定义 + 源码(标出处) + 类比 + 细节。│
│   ❗不是"学习者问一句答一句"——该讲的细节一个不漏，│
│     即使学习者没问到也要主动覆盖。             │
│   每讲完一节 → 出 1 道【微检查点】小问题，      │
│     确认懂了再进下一节。                       │
│   学习者可随时追问；追问的答案 patch 进 dayN.md。│
└─────────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────────┐
│ Step 4 · 动手实验（demos/dayN/）              │
│   AI 给实验任务，学习者自己写/跑/观察。        │
│   产出：代码 + observations.md + 截图         │
│   实验类型：验证型 / 反例型 / DevTools 型      │
└─────────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────────┐
│ Step 5 · Code Review + Bug 复盘               │
│   学习者贴代码 → AI 指 bug → 学习者改 → AI 再 review。 │
│   不直接给改后代码，引导学习者自己改。         │
│   ★ AI 记录每个 bug 及修复过程，写入 summary。 │
└─────────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────────┐
│ Step 6 · 输出精简笔记 + 自测题（学完后）       │
│   ★ 先做"首尾呼应总结"：回到 Step 2 入场自测的 │
│     题目，让学习者重答一遍，对比开头那次的答案， │
│     直观看到当天成长。                         │
│   AI 必须同时生成 2 个文件：                  │
│                                              │
│   notes/dayN-summary.md（精简笔记）：         │
│   • 当日核心要点（用速查表/口诀，200-400 行）  │
│   • ★"我的疑问追问记录"：跟练时学习者问的关键问题 │
│   • ★"我的踩坑记录"：跟练时的代码 bug + 认知误区 │
│   • 认知纠正 5 条（同步追加到 meta/）          │
│                                              │
│   notes/dayN-quiz.md（自测题）：              │
│   • 6-10 题，答案折叠                         │
│   • 题目要覆盖学习者答错过的点                 │
└─────────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────────┐
│ Step 7 · git commit & push                    │
│   Commit message 格式：                       │
│     W{周}D{天} {主题}：{核心动作}              │
│   例：W1 D2 Fiber 双缓存：完成 DevTools 字段拆解 │
│   例：W1 D2 Fiber 双缓存：完成 DevTools 字段拆解 │
└─────────────────────────────────────────────┘
```

---

## 四、AI 协作契约（硬规则）

### ✅ 必须做

1. **不直接给答案**：所有思考题、动手任务，**先让学习者做、再指错**。
2. **代码先讲思路**：贴代码前必须先讲清"为什么这么写、还有什么别的写法、取舍是什么"。
3. **每天产出 4 件套**：`notes/dayN.md`（完整教程） + `demos/dayN/`（实验） + `notes/dayN-summary.md`（精简笔记含疑问/踩坑） + `notes/dayN-quiz.md`（自测题）。
4. **末尾必带预告**：`notes/dayN.md` 末尾必须有 "Day N+1 预告" + 4 个预读问题。
5. **认知纠正归档**：每天提取 3-5 条"我之前以为…其实是…"，追加到 `meta/cognitive-corrections.md`。
6. **跨电脑续接**：学习者说"开始第 N 天"时，AI 自己 `git pull` 对齐进度，不要追问。
7. **跟练过程必须记录**：AI 全程记录学习者答错的问题、追问的关键点、写代码的踩坑，**这些是 summary 的核心素材**。学完后必须真实回填到 `dayN-summary.md` 的"我的疑问追问记录"和"我的踩坑记录"两个章节。
8. **★ 直写文件，不在对话里 dump ★**（节省 token + 保留首次输出）：
   - 跟练开始 → AI 立即把完整教程写进 `notes/dayN.md` 并 commit/push，对话里只发简短提示
   - 学习者答自测题后 → AI 直接 patch 进 `notes/dayN.md` 的"自测点评"小节，对话里只发 200 字内反馈
   - 学习者追问 → AI 直接追加进 `notes/dayN.md` 的"追问 N"小节，对话里只发要点
   - 整个跟练过程 = 对话里短反馈 + 文件随时 commit
   - **禁止**在对话里大段 dump 教程后再"等学完一次性整理到文件"——这会浪费 token + 丢失首次输出
9. **★ 系统完整讲解，不"面向答案挤牙膏" ★**（2026-06-22 学习者反馈 Day5-7 退化后新增）：
   - dayN.md 教程必须**自身完整**——一个从没问过问题的人读它也能学全，不依赖对话补充。
   - Step 3 讲解阶段 AI **主动按章节顺序逐节讲全**，每节覆盖：定义 + 源码(标文件路径+函数名) + 类比 + 易错细节 + 边界情况。
   - ❗**严禁**把"讲解"退化成"学习者问什么才答什么"——没问到的核心细节也要主动讲。
   - 判断标准：如果某个细节只在"追问"里出现、教程正文没有，就是不合格，必须补进正文。
   - 每节讲完出 1 道微检查点小问题，确认懂了再进下一节（避免学到一半才发现"乱了"）。

### ❌ 禁止做

1. ❌ **禁止贴完整可抄代码骨架**当作"思考提示"。
2. ❌ **禁止跳过实验直接讲完理论**。
3. ❌ **禁止用"差不多就是这样"敷衍**，源码层面的事必须精确。
4. ❌ **禁止追问"你想学什么"**——按 `meta/roadmap.md` 走。
5. ❌ **禁止把 day2、day3 内容塞进 dayN.md**，每天就讲当天的。
6. ❌ **禁止跳过 `dayN-summary.md`**——这是 4 件套里最重要的一份（学习者复习时只看这个）。
7. ❌ **禁止把 summary 写成 dayN.md 的复制**——summary 是"压缩 + 个人化"，不是"删减"。
8. ❌ **禁止"面向答案"讲解**——教程正文必须系统完整，不能只覆盖学习者追问到的点。源码细节宁可多讲，不可遗漏。

---

## 五、总学习路线图（roadmap）

> 完整 8 周，每天 2-4 小时。详见 `meta/roadmap.md`。

| 周 | 主题 | Day 范围 |
|---|---|---|
| W1 | JSX → Element → Fiber → 调度 | D1-D5 |
| W2 | Hooks 实现原理（useState/useEffect/useRef/useMemo） | D6-D10 |
| W3 | 并发渲染 Lane + Suspense + Transition | D11-D15 |
| W4 | 状态库源码（Redux Toolkit / Zustand） | D16-D20 |
| W5 | AI Agent 工程化（流式输出 / Tool Calling / ReAct） | D21-D25 |
| W6 | 自研一个 mini-react（500 行复刻） | D26-D30 |
| W7 | 自研一个 AI Agent SDK（基于 React 19 新特性） | D31-D35 |
| W8 | 综合项目 + 面试包装 | D36-D40 |

---

## 六、Commit Message 规范

```
W1 D2 Fiber 双缓存：完成 DevTools 字段拆解
^^ ^^ ^^^^^^^^^^^^^^  ^^^^^^^^^^^^^^^^^^^^^
周 天  主题             核心动作

可选多行：
W1 D2 Fiber 双缓存：完成 DevTools 字段拆解

- 验证 alternate.alternate === self
- 手写 Fiber 树遍历函数
- 记录 5 条认知纠正
```

---

## 七、给"明天的 AI 助手"的话

如果你是后续接手的 AI 助手（同一个或不同设备）：

1. **不要重新提问学习者偏好**——本文档就是答案。
2. **不要重新规划学习路线**——`meta/roadmap.md` 已定。
3. **不要重新建立"教练式引导"约定**——本文档第四节已明确。
4. **不要因为"上下文不全"就要求学习者重新讲一遍**——`git pull` 后所有进度都在仓库里。

学习者花了时间把规则写下来，就是不想再讲第二遍。**尊重这个时间投资。**

---

_最后更新：2026-06-18，与 Day 2 教案同步建立。_
